<% @messages.each do |message| %>
<h2><%= message.title %></h2>
<%= link_to "View Message", message_path(message), class: "btn btn-success btn-sm" %>
<% end %>

<br>
<br>
<% if user_signed_in? %>
<%= link_to "New Message",new_message_path, class: "btb btn-primary btn-sm" %>
<% end%>
